# NextAuth Configuration
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-secret-here-generate-with-openssl-rand-base64-32

# Google OAuth (optional)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Database (for local development)
# In production, these are provided by Cloudflare bindings
DATABASE_URL=

# Email Service (optional, for sending claim invitations)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASSWORD=
EMAIL_FROM=noreply@pdumc2001.com

# Cloudflare R2 Storage (for photos/videos)
# Account ID and Bucket name can be literal values, keys should be secret
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_ACCOUNT_ID=
R2_BUCKET_NAME=pdumc-alumni-storage
R2_PUBLIC_DOMAIN=https://pub-xxxxxx.r2.dev
